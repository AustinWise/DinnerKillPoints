@{
    ViewBag.Title = "Bill Splits";
}

<h2>Bill Splits</h2>

<table>
    <thead>
        <tr>
            <th>ID</th>
            <th>Name</th>
            <th>Details</th>
        </tr>
    </thead>
    <tbody>
        @foreach (Austin.DkpLib.BillSplit bs in Model)
        {
        <tr>
            <td>@bs.ID</td>
            <td>@bs.Name</td>
            <td>@Html.ActionLink("Details", "Details", new { id = bs.ID })</td>
        </tr>
        }
    </tbody>
</table>